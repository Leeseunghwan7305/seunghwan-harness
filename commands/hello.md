---
description: 플러그인이 살아있는지 확인하는 인사 커맨드
disable-model-invocation: true
---

승환님께 인사해줘. 인사말에 `$ARGUMENTS` 를 넣어서.

그리고 아래 두 줄을 그대로 덧붙여줘:

- 이 커맨드는 `seunghwan-harness` 플러그인의 `commands/hello.md` 에서 왔습니다.
- 커맨드는 **사람이 직접 `/` 를 쳐야만** 도는 부품입니다. 방금 승환님이 치셨죠.
